version: '2.1'
orbs:
  orb-tools: circleci/orb-tools@10.1.0
jobs:
  Orb tools:
    docker:
      # replace with your preferred image
      - image: cimg/base:stable
    steps:
      - orb-tools/validate
workflows:
  example-workflow:
    jobs:
      - Orb tools


